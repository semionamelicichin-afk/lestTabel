{
  "name": "lest-tabel-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:daily": "playwright test tests/daily-check-in.spec.ts --project=daily-check-in",
    "test:headed": "playwright test tests/daily-check-in.spec.ts --project=daily-check-in --headed",
    "test:auth": "playwright test tests/auth.setup.spec.ts --project=auth-setup",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.3.1",
    "dotenv": "^16.4.5",
    "typescript": "^5.9.2"
  }
}